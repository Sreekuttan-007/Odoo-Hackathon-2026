# Leave unset for local dev — defaults to http://localhost:8000/api.
# Set this when the frontend is deployed separately from the backend.
# VITE_API_BASE_URL=https://your-deployed-backend.example.com/api
