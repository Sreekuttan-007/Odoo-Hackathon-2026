# Build Status

Track module implementation state here.

- **Frontend App Shell**: DONE
- **Backend API Foundation**: DONE
- **Database/Migrations**: DONE
- **Authentication**: DONE
- **User Management**: DONE
- **Employee Management**: DONE (Kanban + List, detail, create/edit, Department/Job Position/Manager/Working Schedule relations, RBAC)
- **Departments**: DONE (minimal CRUD + reusable selector + seed data)
- **Job Positions**: DONE (minimal CRUD + reusable selector + seed data, no dedicated nav page — managed inline via selector "create" action)
- **Contracts**: DONE (list/detail/create, history, overlap validation, period-applicability service, money-safe wage)
- **Working Schedules**: DONE (list/form, weekly pattern lines, derived daily/weekly hours)
- **Attendance**: DONE (global + employee-filtered list, detail, quick Check In/Check Out widget with live elapsed display, HR correction, real derived worked-hours/overtime, one-session/day + overlap protection)
- **Time Off**: DONE (Types configuration, Allocations with approve/refuse and derived balance, Requests with approve/refuse and exactly-once balance consumption, employee-filtered smart action, real RBAC)
- **Salary Structures/Rules**: DONE (ordered rules, FIXED/PERCENTAGE/FORMULA computation via a safe AST-based evaluator, RBAC split between HR_PAYROLL_USER read-only and HR_PAYROLL_MANAGER full CRUD)
- **Payruns/Payslips**: DONE (two-step creation wizard with backend-revalidated eligibility, DRAFT→COMPUTED→VALIDATED→PAID state machine, real preflight blockers/warnings, PayslipLine computation trace, historically-stable snapshots, real PDF generation)
- **Phase 7 — PayTrace**: DONE (deterministic per-payslip calculation explainer at `/payroll/payslips/:id/trace`, rebuilt entirely from persisted PayslipLine snapshots — verified historically stable against later Salary Rule edits; graceful fallback for pre-Phase-7 legacy lines). **AI Narrator (7B)**: implemented but unconfigured in this environment (`ANTHROPIC_API_KEY` unset) — endpoint verified to degrade to `available: false` rather than fail; untested against a live provider response.
- **Phase 8 — Payroll Preflight**: DONE (deterministic payroll readiness & risk engine, `app/services/preflight.py`). Derived — persists nothing, adds no Payrun status. `GET`/`POST /api/payroll/payruns/:id/preflight` returns `readiness` (NOT_RUN / ACTION_REQUIRED / REVIEW_RECOMMENDED / READY), severity counts, and normalized findings. 13 registered checks across contract / config / integrity / attendance / time-off / variance / duplicates dimensions, reusing the canonical contract-applicability + overlap rules and bridging the compute engine's own BLOCKER `PayrollWarning`s. **Validation gate**: `validate_payrun` recomputes every Payslip then re-runs the Preflight engine server-side and refuses (`409 VALIDATION_BLOCKED`, now with `details.findings`) on any blocker — a stale UI "READY" cannot pass. Preflight panel on the Payrun detail page: readiness banner, blocker/warning/info counts, severity filters, per-finding evidence + resolution + deep links (employee / contracts / payslip / PayTrace), "Run again", and a disabled Validate button while blockers exist. No AI anywhere in the engine. Migration: none (fully derived).
- **Phase 8.5 — Neon Cloud DB Migration & Deployment Hardening**: infra work DONE, cloud connectivity NOT YET VERIFIED (no Neon `DATABASE_URL` provided in this environment). What's done: confirmed the existing stack (SQLAlchemy + `psycopg[binary]` v3 + Alembic reading `DATABASE_URL` from `.env`, `pool_pre_ping=True` already set) needs zero code changes to work against Neon — no URL-parsing hacks, no new driver. Rewrote `backend/seed.py` (previously non-idempotent, and had gone missing from git history entirely) as a fully idempotent get-or-create script, verified by running it twice against the local dev DB and diffing row counts across all 14 tables — byte-identical. Fixed the frontend's hardcoded `http://localhost:8000/api` to read `VITE_API_BASE_URL` with that same value as fallback. Documented the full setup/migration/seed/troubleshooting flow in `docs/NEON_DEPLOYMENT.md`. Test isolation confirmed already safe by construction — `backend/tests/conftest.py` uses a hardcoded `sqlite:///:memory:` engine that never reads `DATABASE_URL`, so pytest structurally cannot touch a Neon database regardless of what's in `.env`. **Remaining**: actual Neon connectivity test, `alembic upgrade head` against Neon, and the full smoke-test checklist (auth/employees/contracts/payroll/PayTrace/Preflight/PDF) — all blocked on the user providing a real Neon `DATABASE_URL`, which per this phase's own spec must go directly into `.env`, never through chat.
- **Phase 9 — Payroll Simulator**: DONE. Deterministic what-if engine reusing the exact canonical payroll engine — no second calculator. Refactored `payroll_engine.py`'s calculation core into `build_calculation_context()` + `execute_rules()` (both now public/reusable), with `compute_payslip()` reduced to a thin persistence wrapper around them; all 169 pre-existing tests passed unchanged after the refactor, proving it's behavior-preserving. `POST /api/payroll/simulator/run` (`HR_PAYROLL_MANAGER`/`ADMIN` only) takes a structure, period, employee list, and rule overrides; overridden rules are represented as transient `SalaryRule` instances never passed to `db.add()` — plain Python objects the ORM session never tracks, so nothing can leak to the database by construction. Returns per-employee current/simulated totals, component-level deltas (recalculated through the real dependency chain, not a hand-added delta), an aggregate company-wide impact, and an optional annualized estimate (only shown when the period looks like one calendar month, clearly labeled as an assumption). Frontend: `/payroll/simulator` — scope + scenario editor (inline per-rule override forms) + impact summary + employee table with a click-to-expand component-comparison inspector; mandatory "simulation only" notice; Reset Scenario / Discard, no Save/Apply/Publish anywhere. **Mandatory DB non-mutation test**: snapshots SalaryRule/Structure/Contract/Payslip/Warning/Attendance/TimeOff state before and after a multi-override, multi-employee simulation and asserts byte-identical — passing. Full regression (new real Payrun → Compute → PayTrace → Preflight → Validate → Mark Paid → PDF) re-verified against the live Neon (Singapore) database after this phase. Migration: none (no new columns).
